from django.shortcuts import render

# Create your views here.
from .models import User, Products

def products_data(request, product_name, value_product, image_product):
    pass

def user_login(request, name, email, password):
    pass